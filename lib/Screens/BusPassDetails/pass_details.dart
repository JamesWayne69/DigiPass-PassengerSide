import 'package:flutter/material.dart';
import 'package:flutter_auth/Screens/BusPassDetails/components/body.dart';

class PassDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
