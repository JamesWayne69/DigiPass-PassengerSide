import 'package:flutter/material.dart';
import 'package:flutter_auth/Screens/Payment/components/body.dart';

class Payment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
